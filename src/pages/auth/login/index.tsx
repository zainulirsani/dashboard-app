import LoginViews from "@/views/Auth/Login";

const LoginPage = () => {
  return (
    <>
      <LoginViews />
    </>
  );
};

export default LoginPage;

// Tidak perlu menggunakan getServerSideProps di sini
